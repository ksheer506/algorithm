const LEFT = ['[', '{', '(']

const RIGHT = [']', '}', ')']

function solution(s) {
  
}

const isBracketNormal = (b) => {
  let r = false
  
  for (let i = 0; i < b.length; i++) {
    // 1. LEFT 원소
  }
}
1 -3 3 -1
const s = "[)(]";
console.log(solution(s));

/* 
스택?
*/
